# TASK-007 — Full-service orchestrator support

**User story.** As a multi-repo operator, I want deeper, fuller
support for orchestrators coordinating work across the system.

**Why.** The current orchestrator touchpoints (the read-only
`.claude/active-*.md` notices in `task-rules.md`) are thin; richer
orchestrator support would make cross-repo coordination
first-class.

**Notes (raw idea).** A deeper, better, more full-service support
for orchestrators. Builds on the existing "Active orchestrator
notices" section in `task-rules.md`.

STATUS: STUB
