---
name: <kebab-case-test-name>
kind: test
test_kind: integration
language: <bash | typescript | python | swift | ...>
location: <path to the test source>
run_command: "bash tests/scripts/<endpoint>.sh"
runtimes_required: [api]
created: <YYYY-MM-DD>
status: active                        # active / quarantined / retired
tags: []
---

# <Name>

> Template — copy to `tests/stamps/<YYYY-MM-DD>-<name>.md`, fill the
> fields, then add `name` to the `tests:` list of **`tests/suites/e2e.md (needs a runtime) or pre-deploy.md (in-process)`**.
> One stamp format for every test: the one `build/stages/30-test.sh` reads
> (`test-rules.md`). `/test add` walks you through it.

## What this verifies

<one API endpoint: the request, and the response shape it must return.>

## How it runs

1. Against a running server → `test_kind: e2e`, member of `e2e.md`, with
   the server in its `runtimes:` list.
   In-process (a test client, no server) → `test_kind: integration`, member
   of `pre-deploy.md`, `runtimes_required: []`.
2. The script doubles as a reference client: keep the request readable.

`run_command` runs from the project root and must exit non-zero on failure.
Run it by hand once before adding it to the suite.

## Why it matters

<What breaks for a user if this fails.>
